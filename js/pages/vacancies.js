// Экспортируем функцию инициализации для module режима
window.initVacanciesPage = function() {
  // Логика для передачи данных о вакансии в модальное окно
  // Теперь обрабатывается централизованно в ModalManager через data-modal-open
  Logger.INFO('Страница вакансий инициализирована');
};

/**
 * Очистка ресурсов страницы вакансий (пустая функция для совместимости)
 */
window.destroyVacanciesPage = function() {
  // Страница вакансий не создаёт собственных обработчиков
  // Все обработчики делегированы в ModalManager через data-modal-open
};
